
<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All our flats</h1>
    </div>
  </div>
  <div class="row">
    <div class="col">
      <p>All <%= @flats.count %> flats.</p>
    </div>
  </div>

  <div class="row">
    <% @flats.each do |flat| %>
      <div class="col-4">
        <div class="flat-card">
          <div class="flat-card-content">
            <div class="flat-card-title">
              <h6><%= flat.name %></h6>
            </div>
            <p><%= flat.address %></p>
            <div class="flex info">
              🧑‍🤝‍🧑<span class="ml-2"><%= flat.number_of_guests %> people</span>
              <br>
              💰<span class="ml-2"><%= flat.price_per_night %> €</span>
            </div>
          </div>
          <div class="flat-card-actions">
            <%= link_to "More info", flat_path(flat) %>
            <%= link_to "Edit",edit_flat_path(flat), class:"text-warning ml-3" %>
            <%= link_to "Remove", flat_path(flat), method: :delete, class:"text-danger ml-3" %>
          </div>
      </div>
    </div>
      <% end %>

  </div>
</div>




